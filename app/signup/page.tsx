import Link from "next/link";
import { signup } from "@/app/actions/auth";

export default async function SignupPage({ searchParams }: { searchParams: Promise<{ error?: string }> }) {
  const { error } = await searchParams;
  return <main className="flex min-h-screen items-center justify-center px-6 py-12"><div className="w-full max-w-md rounded-[36px] bg-white p-8 shadow-xl shadow-black/5 md:p-10"><Link href="/" className="font-semibold tracking-[0.25em]">AARYVO</Link><p className="mt-12 text-sm uppercase tracking-[0.2em] text-black/40">Get started</p><h1 className="mt-3 text-4xl font-semibold tracking-[-0.05em]">Build your first AI employee.</h1>{error && <p className="mt-5 rounded-2xl bg-red-50 px-4 py-3 text-sm text-red-700">Enter a valid name, email and password of at least 8 characters.</p>}<form action={signup} className="mt-8 space-y-4"><input name="name" required placeholder="Your name" className="w-full rounded-2xl border border-black/10 px-5 py-4 outline-none focus:border-black/30"/><input name="email" type="email" required placeholder="Work email" className="w-full rounded-2xl border border-black/10 px-5 py-4 outline-none focus:border-black/30"/><input name="password" type="password" minLength={8} required placeholder="Password · minimum 8 characters" className="w-full rounded-2xl border border-black/10 px-5 py-4 outline-none focus:border-black/30"/><button className="w-full rounded-full bg-black px-6 py-4 font-medium text-white">Create account</button></form><p className="mt-6 text-center text-sm text-black/50">Already have an account? <Link href="/login" className="font-medium text-black">Sign in</Link></p></div></main>;
}
