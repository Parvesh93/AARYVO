import { emailAnalyticsSummary } from "@/lib/email-analytics";
export default async function EmailAnalyticsPage(){const d=await emailAnalyticsSummary(30);const pct=(v:number)=>d.sent?`${(v/d.sent*100).toFixed(1)}%`:"0%";return <div><p className="text-xs font-semibold uppercase tracking-[.18em] text-black/35">Communication</p><h1 className="mt-2 text-4xl font-semibold tracking-[-.045em]">Email analytics</h1><p className="mt-2 text-sm text-black/45">AARYVO SMTP activity and engagement · last 30 days.</p><section className="mt-8 grid gap-3 sm:grid-cols-2 xl:grid-cols-5">{[["Emails",d.total],["Sent",d.sent],["Failed",d.failed],["Open rate",pct(d.opened)],["Click rate",pct(d.clicked)]].map(([l,v])=><div key={l as string} className="rounded-3xl border border-black/[.055] bg-white p-5"><p className="text-xs text-black/35">{l}</p><p className="mt-3 text-2xl font-semibold tracking-[-.04em]">{v}</p></div>)}</section><div className="mt-4 overflow-hidden rounded-3xl border border-black/[.055] bg-white"><div className="border-b border-black/[.055] px-6 py-5"><h2 className="font-semibold">Email categories</h2><p className="mt-1 text-xs text-black/35">Sent/failed tracking becomes available as AARYVO emails are routed through the analytics wrapper.</p></div><table className="w-full text-left text-sm"><thead className="bg-black/[.018] text-[10px] uppercase tracking-[.12em] text-black/35"><tr><th className="px-6 py-3">Category</th><th>Total</th><th>Sent</th><th>Failed</th></tr></thead><tbody>{d.byCategory.map(r=><tr key={r.category} className="border-t border-black/[.05]"><td className="px-6 py-4 font-medium">{r.category}</td><td>{r.total}</td><td>{r.sent}</td><td>{r.failed}</td></tr>)}</tbody></table></div><p className="mt-4 text-xs leading-5 text-black/35">SMTP “Sent” means the configured mail server accepted the message. Open/click tracking will be added to HTML emails next; it should not be interpreted as guaranteed inbox delivery.</p></div>}
