import {NextResponse} from "next/server";import {getSession} from "@/lib/auth";import {prisma} from "@/lib/prisma";import {isPlanKey,razorpayConfigured,razorpayPlanId,razorpayRequest} from "@/lib/billing";
export async function POST(request:Request){const session=await getSession();if(!session)return NextResponse.json({error:"Unauthorized"},{status:401});const member=await prisma.businessMember.findFirst({where:{userId:session.userId},include:{user:true,business:true}});if(!member)return NextResponse.json({error:"Workspace not found"},{status:404});if(!razorpayConfigured())return NextResponse.json({error:"Billing is not configured yet."},{status:503});const body=await request.json(),plan=String(body.plan||"").toUpperCase();if(!isPlanKey(plan)||plan==="FREE")return NextResponse.json({error:"Choose a paid plan."},{status:400});const planId=razorpayPlanId(plan);if(!planId)return NextResponse.json({error:`Razorpay ${plan} plan is not configured.`},{status:503});try{const subscription=await razorpayRequest("/subscriptions",{method:"POST",body:JSON.stringify({plan_id:planId,total_count:120,quantity:1,customer_notify:1,notes:{businessId:member.businessId,plan}})});await prisma.business.update({where:{id:member.businessId},data:{razorpaySubscriptionId:subscription.id,razorpayPlanId:planId,subscriptionStatus:subscription.status||"created"}});return NextResponse.json({subscriptionId:subscription.id,keyId:process.env.RAZORPAY_KEY_ID,name:member.business.name,email:member.user.email,plan})}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"Unable to start subscription."},{status:500})}}
