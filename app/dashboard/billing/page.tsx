import {redirect} from "next/navigation";import {requireSession} from "@/lib/auth";import {prisma} from "@/lib/prisma";import BillingClient from "./BillingClient";
export default async function BillingPage(){const session=await requireSession();const member=await prisma.businessMember.findFirst({where:{userId:session.userId},include:{business:{include:{agents:{select:{id:true}}}}}});if(!member)redirect("/onboarding");const b=member.business,ids=b.agents.map(a=>a.id);const start=b.usagePeriodStart||new Date(new Date().getFullYear(),new Date().getMonth(),1),end=b.usagePeriodEnd||new Date(new Date().getFullYear(),new Date().getMonth()+1,1);const usage=await prisma.conversation.count({where:{agentId:{in:ids},startedAt:{gte:start,lt:end}}});return <div className="mx-auto max-w-[1400px] pb-8"><p className="text-xs font-semibold uppercase tracking-[.16em] text-black/35">Subscription</p><h1 className="mt-2 text-3xl font-semibold tracking-[-.04em] sm:text-4xl">Billing & plan</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-black/45">Choose the AARYVO plan that matches your website conversation volume. Usage resets each billing period.</p><BillingClient currentPlan={b.plan} usage={usage} limit={b.monthlyConversationLimit}/></div>}
