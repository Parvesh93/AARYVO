import { redirect } from "next/navigation";
import AgentTester from "@/app/dashboard/AgentTester";
import { requireSession } from "@/lib/auth";
import { prisma } from "@/lib/prisma";
export default async function AIEmployeePage(){const session=await requireSession();const member=await prisma.businessMember.findFirst({where:{userId:session.userId},include:{business:{include:{agents:{take:1,include:{_count:{select:{knowledgeItems:true,conversations:true}}}}}}}});if(!member)redirect("/onboarding");const b=member.business,agent=b.agents[0];return <div className="mx-auto max-w-[1200px]"><p className="text-xs font-semibold uppercase tracking-[.16em] text-black/35">Your AI sales agent</p><h1 className="mt-2 text-3xl font-semibold tracking-[-.04em] sm:text-4xl">AI Employee</h1><p className="mt-2 text-sm text-black/45">Test how your AI employee responds before customers speak with it.</p><div className="mt-7 grid gap-4 sm:grid-cols-3"><div className="rounded-2xl border border-black/[.06] bg-white p-5"><p className="text-xs text-black/35">Agent</p><p className="mt-2 font-semibold">{agent?.name||"Not configured"}</p></div><div className="rounded-2xl border border-black/[.06] bg-white p-5"><p className="text-xs text-black/35">Knowledge</p><p className="mt-2 font-semibold">{agent?._count.knowledgeItems||0} pages</p></div><div className="rounded-2xl border border-black/[.06] bg-white p-5"><p className="text-xs text-black/35">Conversations</p><p className="mt-2 font-semibold">{agent?._count.conversations||0}</p></div></div>{agent?<div className="mt-4"><AgentTester businessName={b.name}/></div>:<div className="mt-4 rounded-2xl border border-black/[.06] bg-white p-10 text-center text-sm text-black/40">Complete onboarding to create your AI employee.</div>}</div>}
