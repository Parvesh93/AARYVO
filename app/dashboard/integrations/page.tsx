import { redirect } from "next/navigation";
import { requireSession } from "@/lib/auth";
import { prisma } from "@/lib/prisma";
export default async function IntegrationsPage(){const session=await requireSession();const member=await prisma.businessMember.findFirst({where:{userId:session.userId}});if(!member)redirect("/onboarding");return <div className="mx-auto max-w-[1100px]"><p className="text-xs font-semibold uppercase tracking-[.16em] text-black/35">Connected apps</p><h1 className="mt-2 text-3xl font-semibold tracking-[-.04em] sm:text-4xl">Integrations</h1><p className="mt-2 text-sm text-black/45">AARYVO is starting with website conversion and direct human handoff. More connected apps arrive in Phase 2.</p><div className="mt-7 grid gap-4 md:grid-cols-2"><div className="rounded-2xl border border-black/[.06] bg-white p-6"><div className="flex items-start justify-between gap-4"><div className="flex h-11 w-11 items-center justify-center rounded-xl bg-[#eaf8ef] text-lg font-bold text-[#128c4b]">W</div><span className="rounded-full bg-emerald-50 px-3 py-1.5 text-[11px] font-semibold text-emerald-700">Available now</span></div><h2 className="mt-6 text-lg font-semibold">WhatsApp handoff</h2><p className="mt-2 text-sm leading-6 text-black/45">Let qualified website visitors continue the conversation directly with your team on WhatsApp.</p><a href="/dashboard/widget" className="mt-6 inline-flex rounded-full bg-black px-4 py-2 text-xs font-medium text-white">Configure in Widget</a></div><div className="rounded-2xl border border-dashed border-black/[.1] bg-white/50 p-6"><div className="flex items-start justify-between gap-4"><div className="flex h-11 w-11 items-center justify-center rounded-xl bg-[#f5f6f8] text-lg font-bold">G</div><span className="rounded-full bg-black/[.04] px-3 py-1.5 text-[11px] font-semibold text-black/40">Coming soon · Phase 2</span></div><h2 className="mt-6 text-lg font-semibold">Google Calendar</h2><p className="mt-2 text-sm leading-6 text-black/45">Calendar sync, availability checks and automatic event creation are planned for Phase 2.</p></div></div></div>}
