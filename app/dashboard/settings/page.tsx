import { notFound } from "next/navigation";
import { requireSession } from "@/lib/auth";
import { isGoogleOAuthConfigured } from "@/lib/google-oauth";
import { prisma } from "@/lib/prisma";
import CalendarConnection from "./CalendarConnection";
import SettingsForm from "./SettingsForm";
export default async function SettingsPage(){const session=await requireSession();const member=await prisma.businessMember.findFirst({where:{userId:session.userId},include:{business:true}});if(!member)notFound();const b=member.business;return <div className="mx-auto max-w-[1100px]"><p className="text-xs font-semibold uppercase tracking-[.16em] text-black/35">Workspace</p><h1 className="mt-2 text-3xl font-semibold tracking-[-.04em] sm:text-4xl">Settings</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-black/45">Manage notifications, booking availability and connected services for {b.name}.</p><div className="mt-7 space-y-5"><CalendarConnection connected={Boolean(b.googleRefreshToken&&b.googleCalendarId)} accountEmail={b.googleAccountEmail} calendarName={b.googleCalendarName} oauthReady={isGoogleOAuthConfigured()}/><SettingsForm initial={{notificationEmail:b.notificationEmail,notifyNewLead:b.notifyNewLead,notifyHotLead:b.notifyHotLead,notifyAppointment:b.notifyAppointment,notifyHumanAttention:b.notifyHumanAttention,bookingTimeZone:b.bookingTimeZone,bookingSlotMinutes:b.bookingSlotMinutes,bookingStartHour:b.bookingStartHour,bookingEndHour:b.bookingEndHour,bookingDaysAhead:b.bookingDaysAhead,bookingWorkingDays:b.bookingWorkingDays}}/></div></div>}
