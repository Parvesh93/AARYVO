"use client";
import { useState } from "react";

type Props={connected:boolean;accountEmail:string|null;calendarName:string|null;oauthReady:boolean};
export default function CalendarConnection({connected,accountEmail,calendarName,oauthReady}:Props){const[disconnecting,setDisconnecting]=useState(false);async function disconnect(){setDisconnecting(true);try{const res=await fetch("/api/integrations/google/disconnect",{method:"POST"});if(!res.ok)throw new Error();window.location.href="/dashboard/settings?calendar=disconnected";}catch{setDisconnecting(false);}}
return <section className="rounded-3xl border border-black/10 bg-white p-6 md:p-8"><div className="flex flex-col gap-5 md:flex-row md:items-center md:justify-between"><div><div className="flex items-center gap-2"><h2 className="text-xl font-semibold">Google Calendar</h2><span className={`rounded-full px-2.5 py-1 text-[11px] font-semibold ${connected?"bg-emerald-50 text-emerald-700":"bg-black/5 text-black/50"}`}>{connected?"Connected":"Not connected"}</span></div><p className="mt-2 max-w-2xl text-sm leading-6 text-black/55">AARYVO checks busy times before showing slots and adds confirmed consultation requests to your calendar.</p>{connected&&<p className="mt-3 text-sm text-black/70"><strong>{calendarName||"Google Calendar"}</strong>{accountEmail?` · ${accountEmail}`:""}</p>}{!oauthReady&&<p className="mt-3 text-xs text-amber-700">Google OAuth needs to be configured by the AARYVO administrator before customers can connect.</p>}</div><div className="shrink-0">{connected?<button type="button" disabled={disconnecting} onClick={disconnect} className="rounded-full border border-black/15 px-5 py-2.5 text-sm font-semibold hover:bg-black/5 disabled:opacity-50">{disconnecting?"Disconnecting…":"Disconnect"}</button>:<a href={oauthReady?"/api/integrations/google/connect":"#"} aria-disabled={!oauthReady} className={`inline-flex rounded-full px-5 py-2.5 text-sm font-semibold ${oauthReady?"bg-black text-white":"cursor-not-allowed bg-black/10 text-black/35"}`}>Connect Google Calendar</a>}</div></div></section>}
