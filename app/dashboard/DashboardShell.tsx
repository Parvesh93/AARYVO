"use client";
import Link from "next/link";import {usePathname} from "next/navigation";import {useState} from "react";import {Bot,CalendarDays,ChevronDown,CreditCard,Database,Home,Inbox,Menu,MessageCircle,Plug,Settings,Sparkles,Users,X} from "lucide-react";import {logout} from "@/app/actions/auth";
type Props={children:React.ReactNode;user:{name:string|null;email:string};business:{name:string;websiteUrl:string|null}};
const NAV=[{href:"/dashboard",label:"Overview",icon:Home},{href:"/dashboard/conversations",label:"Conversations",icon:Inbox},{href:"/dashboard/leads",label:"Leads",icon:Users},{href:"/dashboard/appointments",label:"Appointments",icon:CalendarDays},{href:"/dashboard/knowledge",label:"Knowledge",icon:Database},{href:"/dashboard/ai-employee",label:"AI Employee",icon:Bot},{href:"/dashboard/widget",label:"Widget",icon:MessageCircle},{href:"/dashboard/integrations",label:"Integrations",icon:Plug},{href:"/dashboard/billing",label:"Billing",icon:CreditCard}];
function initials(n:string|null,e:string){return(n?.trim()||e).split(/\s+|@/).filter(Boolean).slice(0,2).map(p=>p[0]?.toUpperCase()).join("")||"A"}
export default function DashboardShell({children,user,business}:Props){const pathname=usePathname(),[mobileOpen,setMobileOpen]=useState(false),[profileOpen,setProfileOpen]=useState(false);const active=(h:string)=>h==="/dashboard"?pathname===h:pathname.startsWith(h);const Sidebar=({mobile=false}:{mobile?:boolean})=><aside className={`${mobile?"flex":"hidden lg:flex"} h-full w-[268px] shrink-0 flex-col overflow-hidden border-r border-white/[0.06] bg-[#111319] text-white`}><div className="flex h-20 shrink-0 items-center justify-between px-6"><Link href="/dashboard" onClick={()=>setMobileOpen(false)} className="flex items-center gap-3"><span className="flex h-9 w-9 items-center justify-center rounded-xl bg-white text-black"><Sparkles size={17}/></span><div><p className="font-semibold tracking-[0.22em]">AARYVO</p><p className="mt-0.5 text-[10px] text-white/35">AI SALES OS</p></div></Link>{mobile&&<button onClick={()=>setMobileOpen(false)} className="p-2"><X size={18}/></button>}</div><div className="shrink-0 px-4 pb-3 pt-2"><div className="rounded-2xl border border-white/10 bg-white/[0.06] px-4 py-3"><p className="text-[10px] uppercase tracking-[0.16em] text-white/35">Workspace</p><p className="mt-1.5 truncate text-sm font-medium">{business.name}</p><p className="mt-1 truncate text-[11px] text-white/35">{business.websiteUrl||"Website not set"}</p></div></div><nav className="min-h-0 flex-1 space-y-1 overflow-y-auto px-3 py-3">{NAV.map(i=>{const Icon=i.icon,a=active(i.href);return <Link key={i.href} href={i.href} onClick={()=>setMobileOpen(false)} className={`flex items-center gap-3 rounded-xl px-3 py-2.5 text-sm transition ${a?"bg-white text-black":"text-white/55 hover:bg-white/[.07] hover:text-white"}`}><Icon size={17}/>{i.label}</Link>})}</nav><div className="shrink-0 border-t border-white/[.08] p-3"><Link href="/dashboard/settings" className={`mb-2 flex items-center gap-3 rounded-xl px-3 py-2.5 text-sm ${pathname.startsWith("/dashboard/settings")?"bg-white text-black":"text-white/55"}`}><Settings size={17}/>Settings</Link><div className="relative"><button onClick={()=>setProfileOpen(v=>!v)} className="flex w-full items-center gap-3 rounded-xl px-3 py-2.5 text-left hover:bg-white/[.07]"><span className="flex h-9 w-9 items-center justify-center rounded-full bg-white text-xs font-semibold text-black">{initials(user.name,user.email)}</span><span className="min-w-0 flex-1"><span className="block truncate text-sm">{user.name||"Account"}</span><span className="block truncate text-[11px] text-white/35">{user.email}</span></span><ChevronDown size={15}/></button>{profileOpen&&<div className="absolute bottom-[58px] left-0 right-0 rounded-2xl bg-[#1b1e25] p-2"><Link href="/dashboard/settings" className="block rounded-xl px-3 py-2 text-sm text-white/65">Profile & settings</Link><form action={logout}><button className="w-full rounded-xl px-3 py-2 text-left text-sm text-red-300">Sign out</button></form></div>}</div></div></aside>;return <div className="h-dvh overflow-hidden bg-[#f5f6f8] text-[#111319]"><div className="flex h-full min-h-0 overflow-hidden"><Sidebar/>{mobileOpen&&<div className="fixed inset-0 z-50 lg:hidden"><button className="absolute inset-0 bg-black/40" onClick={()=>setMobileOpen(false)}/><div className="relative h-full"><Sidebar mobile/></div></div>}<div className="flex min-w-0 flex-1 flex-col overflow-hidden"><header className="flex h-16 shrink-0 items-center justify-between border-b border-black/[.06] px-4 sm:px-6 lg:px-8"><div className="flex items-center gap-3"><button onClick={()=>setMobileOpen(true)} className="rounded-xl border bg-white p-2 lg:hidden"><Menu size={18}/></button><div><p className="text-sm font-medium">{business.name}</p><p className="hidden text-[11px] text-black/35 sm:block">AARYVO workspace</p></div></div><div className="flex gap-2"><Link href="/dashboard/ai-employee" className="hidden rounded-full border bg-white px-4 py-2 text-xs sm:inline-flex">Test AI employee</Link><Link href="/dashboard/settings" className="flex h-9 w-9 items-center justify-center rounded-full bg-black text-xs font-semibold text-white">{initials(user.name,user.email)}</Link></div></header><main className="min-h-0 flex-1 overflow-y-auto px-4 py-6 sm:px-6 lg:px-8 lg:py-8">{children}</main></div></div></div>}
